const {
  buildAuthCookie,
  clearAuthCookie,
  createToken,
  loginUser,
  registerUser,
  getUserFromRequest
} = require('../../server/authService');

module.exports = {
  buildAuthCookie,
  clearAuthCookie,
  createToken,
  loginUser,
  registerUser,
  getUserFromRequest
};
